# Listing 4: ATFConstants.pm

#!/usr/bin/perl -wT

# Time-stamp: <1998-12-07 19:45:05 reuven>

use strict;
use diagnostics;

# Declare some commonly used variables
use vars qw($database $server $port $username $password $zap_password);

$database = "reuven";
$server = "localhost";
$port = "3306";
$username = "reuven";
$password = "SETME";

$zap_password = "flarg";

# Exit normally;
1;

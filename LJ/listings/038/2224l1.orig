Listing 1.  A sample quizfile.

# This is the quiz file about GNU Emacs.

# Comment lines contain a hash mark (#) in the first column,
# and are ignored, as are lines containing only whitespace.

What key can be used in place of meta?	ESC	Control	Shift	Control-Shift	A
The Web browser written entirely in Emacs Lisp is	Mosaic	Arena	w3-mode	www-mode	C
